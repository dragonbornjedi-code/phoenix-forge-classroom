<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.phoenixforge.profile.READ"
        android:protectionLevel="signature" />

    <application>
        <provider
            android:name="com.phoenixforge.profile.data.provider.ProfileContentProvider"
            android:authorities="com.phoenixforge.profile.provider"
            android:exported="true"
            android:readPermission="com.phoenixforge.profile.READ" />
    </application>

</manifest>
