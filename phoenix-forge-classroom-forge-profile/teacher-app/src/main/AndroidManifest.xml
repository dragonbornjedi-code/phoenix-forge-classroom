<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="com.phoenixforge.profile.READ" />
    <uses-permission android:name="com.phoenixforge.student.sync.READ" />

    <queries>
        <provider android:authorities="com.phoenixforge.profile.provider" />
        <provider android:authorities="com.phoenixforge.student.sync.provider" />
    </queries>

    <application
        android:name=".TeacherEditionApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeacherEdition">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TeacherEdition">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
